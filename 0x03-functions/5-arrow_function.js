/*
- arrow function: returns true if day is Wednesday, otherwise returns false.
*/
const plantNeedsWater = (day) => {
  if (day === "Wednesday") {
    return true;
  } else {
    return false;
  }
};
