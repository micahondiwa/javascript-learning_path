/*
- a function that takes number of rows and columns as inputs and returns the product of rows by columns.
*/

function monitorCount(rows, columns) {
  return rows * columns;
}
const numOfMonitors = monitorCount(5, 4);
console.log(numOfMonitors);
