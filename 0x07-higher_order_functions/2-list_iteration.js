/**
 * Iteration: printing a list through iteration.
 */
const fruits = ["mango", "papaya", "pineapple", "apple"];

// Iterate over fruits below
fruits.forEach(function (item) {
  console.log("I want to eat a " + item);
});
